<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/cardDispute"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp"
    app:cardBackgroundColor="#FFFFFF"
    app:cardCornerRadius="16dp"
    app:cardElevation="0dp"
    app:strokeColor="#E5E7EB"
    app:strokeWidth="1dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Top Header: Issue icon, Title + Order ID, Status Pill -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <!-- Issue Type Icon Badge (38x38) -->
            <FrameLayout
                android:id="@+id/flTypeBadge"
                android:layout_width="38dp"
                android:layout_height="38dp"
                android:layout_marginEnd="12dp"
                android:background="@drawable/bg_badge_blue">

                <ImageView
                    android:id="@+id/ivTypeIcon"
                    android:layout_width="20dp"
                    android:layout_height="20dp"
                    android:layout_gravity="center"
                    android:src="@drawable/ic_more_undo"
                    app:tint="#1A6FD4" />
            </FrameLayout>

            <!-- Title & Order ID -->
            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/tvTypeAndQty"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textColor="#111827"
                    android:textSize="14sp"
                    android:textStyle="bold"
                    tools:text="Return · Qty 1" />

                <TextView
                    android:id="@+id/tvOrderId"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="2dp"
                    android:fontFamily="monospace"
                    android:textColor="#6B7280"
                    android:textSize="12sp"
                    tools:text="Order #72a9092c..." />
            </LinearLayout>

            <!-- Status Pill -->
            <TextView
                android:id="@+id/tvStatusPill"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:paddingHorizontal="10dp"
                android:paddingVertical="4dp"
                android:textColor="#B42318"
                android:textSize="11sp"
                android:textStyle="bold"
                tools:background="#FBE4E1"
                tools:text="ACTION REQUIRED" />

        </LinearLayout>

        <!-- Customer Reason Box -->
        <LinearLayout
            android:id="@+id/layoutReasonBox"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:background="@drawable/bg_rounded_8dp"
            android:backgroundTint="#F9FAFB"
            android:orientation="vertical"
            android:padding="10dp">

            <TextView
                android:id="@+id/tvReasonText"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:maxLines="3"
                android:textColor="#374151"
                android:textSize="13sp"
                tools:text="Item received is completely different from what was ordered in size and color." />

        </LinearLayout>

        <!-- Meta Line: Date & Refund Info -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/tvDate"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:textColor="#6B7280"
                android:textSize="12sp"
                tools:text="20 Aug 2026" />

            <TextView
                android:id="@+id/tvRefundBadge"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="#059669"
                android:textSize="12sp"
                android:textStyle="bold"
                android:visibility="gone"
                tools:text="Refund: ₹850"
                tools:visibility="visible" />
        </LinearLayout>

        <!-- Action Button (Resolve) -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnResolve"
            android:layout_width="match_parent"
            android:layout_height="44dp"
            android:layout_marginTop="12dp"
            android:text="Resolve"
            android:textAllCaps="false"
            android:textColor="#FFFFFF"
            android:textSize="13.5sp"
            android:textStyle="bold"
            app:backgroundTint="#111827"
            app:cornerRadius="10dp"
            app:icon="@drawable/ic_baseline_forum_24"
            app:iconGravity="textStart"
            app:iconSize="16dp"
            app:iconTint="#FFFFFF" />

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
